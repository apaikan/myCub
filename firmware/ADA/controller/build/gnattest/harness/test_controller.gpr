with "aunit";
with "../../../controller.gpr";
project test_controller is

   for Source_Dirs use
     ("../tests",
      "common");

   package Compiler is
      for Default_Switches ("ada") use
        ("-g", "-O1", "-gnatyM0", "-gnata");
   end Compiler;

   package Ide renames controller.Ide;

end test_controller;
